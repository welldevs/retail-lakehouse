# Dependencias de RUNTIME do INE Callejero Source.
#
# Deliberadamente vazio: o pacote so le arquivos locais (biblioteca padrao) — nao ha
# API para este dataset, entao nao ha nem cliente HTTP a instalar. Isso e verificado
# automaticamente por tests/test_dependencies.py, que percorre a AST de todos os
# modulos e falha se aparecer qualquer import fora da stdlib.
#
# Consequencia para o container: a imagem nao precisa instalar nenhum pacote de terceiros.
#
# Se um dia uma dependencia de runtime for necessaria, ela entra AQUI e tambem em
# [project.dependencies] no pyproject.toml. Os dois nao podem divergir.
