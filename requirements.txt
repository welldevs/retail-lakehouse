# Dependencias de RUNTIME do Mercadona Catalog API Source.
#
# Deliberadamente vazio: o pacote usa apenas a biblioteca padrao do Python 3.12.
# Isso e verificado automaticamente por tests/test_dependencies.py, que percorre a AST
# de todos os modulos e falha se aparecer qualquer import fora da stdlib.
#
# Consequencia para o container: a imagem nao precisa instalar nenhum pacote de terceiros.
# Basta python:3.12-slim + o codigo. Ver README.md, secao "Container".
#
# Verificacao:
#   python -m unittest tests.test_dependencies
#   python -c "import importlib.metadata as m; print(m.distribution('mercadona-catalog-source').requires)"
#   -> None
#
# Se um dia uma dependencia de runtime for necessaria, ela entra AQUI e tambem em
# [project.dependencies] no pyproject.toml. Os dois nao podem divergir.
